import {expect, use} from 'chai';
import * as chaiAsPromised from 'chai-as-promised';
import {Config} from './Config';

use(chaiAsPromised);

describe('Config', () => {

    it('#get()', () => {

    });
});
